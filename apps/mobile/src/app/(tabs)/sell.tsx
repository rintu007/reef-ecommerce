import { Text, View } from "react-native";

export default function SellScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-gray-400">Sell — coming in M2</Text>
    </View>
  );
}
