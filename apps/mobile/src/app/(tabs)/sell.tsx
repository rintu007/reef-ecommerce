import { Text, View } from "react-native";

export default function SellScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-muted-foreground">Sell — coming in M2</Text>
    </View>
  );
}
